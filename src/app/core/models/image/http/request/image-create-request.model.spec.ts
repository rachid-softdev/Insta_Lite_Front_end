import { ImageCreateRequest } from './image-create-request.model';

describe('ImageCreateRequest', () => {
  it('should create an instance', () => {
    expect(new ImageCreateRequest()).toBeTruthy();
  });
});
