import { ImageResponseAPI } from './image-response-api.model'

describe('ImageResponseAPI', () => {
  it('should create an instance', () => {
    const instance: ImageResponseAPI = {
      
    };
    expect(instance).toBeTruthy();
  });
});
