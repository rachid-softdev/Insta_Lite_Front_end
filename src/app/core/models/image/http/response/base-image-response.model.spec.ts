import { BaseImageResponse } from './base-image-response.model';

describe('BaseImageResponse', () => {
  it('should create an instance', () => {
    expect(new BaseImageResponse()).toBeTruthy();
  });
});
