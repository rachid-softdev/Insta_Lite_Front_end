import { ImagePageResponse } from './image-page-response.model';

describe('ImagePageResponse', () => {
  it('should create an instance', () => {
    expect(new ImagePageResponse()).toBeTruthy();
  });
});
