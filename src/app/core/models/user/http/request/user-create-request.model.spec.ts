import { UserCreateRequest } from './user-create-request.model';

describe('UserCreateRequest', () => {
  it('should create an instance', () => {
    expect(new UserCreateRequest()).toBeTruthy();
  });
});
