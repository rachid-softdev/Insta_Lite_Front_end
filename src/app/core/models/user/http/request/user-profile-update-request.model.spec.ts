import { UserProfileUpdateRequest } from './user-profile-update-request.model';

describe('UserProfileUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new UserProfileUpdateRequest()).toBeTruthy();
  });
});
