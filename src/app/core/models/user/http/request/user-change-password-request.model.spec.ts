import { UserChangePasswordRequest } from './user-change-password-request.model';

describe('UserChangePasswordRequest', () => {
  it('should create an instance', () => {
    expect(new UserChangePasswordRequest()).toBeTruthy();
  });
});
