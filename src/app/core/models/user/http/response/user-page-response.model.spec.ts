import { UserPageResponse } from './user-page-response.model';

describe('UserPageResponse', () => {
  it('should create an instance', () => {
    expect(new UserPageResponse()).toBeTruthy();
  });
});
