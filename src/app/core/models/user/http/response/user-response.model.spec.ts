import { BaseUserResponse } from './base-user-response.model';

describe('BaseUserResponse', () => {
  it('should create an instance', () => {
    expect(new BaseUserResponse()).toBeTruthy();
  });
});
