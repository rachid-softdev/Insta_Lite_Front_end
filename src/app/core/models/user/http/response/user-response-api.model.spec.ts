import { UserResponseAPI } from './user-response-api.model'

describe('UserResponseAPI', () => {
  it('should create an instance', () => {
    const instance: UserResponseAPI = {
      
    };
    expect(instance).toBeTruthy();
  });
});
