import { ImageUpdateRequest } from './image-update-request.model';

describe('ImageUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new ImageUpdateRequest()).toBeTruthy();
  });
});
