import { LoginUserRequest } from './login-user-request.model';

describe('LoginUserRequest', () => {
  it('should create an instance', () => {
    expect(new LoginUserRequest()).toBeTruthy();
  });
});
